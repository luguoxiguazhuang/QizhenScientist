# Optional idea-generation instructions

You may add extra constraints here, such as target subfield, preferred materials,
experimental limitations, or desired application. The JSON question remains the
primary input. Leave this file unchanged when no extra instructions are needed.
